bcr_test_module:
  module_path: "signing/tests"
  matrix:
    bazel: ["8.*", "9.*"]
    platform: ["debian11", "ubuntu2204", "windows", "macos", "macos_arm64"]
  tasks:
    smoke_tests:
      name: "Run smoke tests"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
